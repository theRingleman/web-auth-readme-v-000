<% if @friends.empty? %>
  <h1>You are a loser and have no friends!</h1>
<% else %>
  <ul>
    <% @friends.each do |friend| %>
      <li><%= "#{friend['firstName']} #{friend['lastName']}" %></li>
    <% end %>
  </ul>
<% end %>
